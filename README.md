# CI/CD Demo – Node.js + Docker + GitHub Actions 🚀

- Automated Docker builds
- CI/CD pipeline using **GitHub Actions**
- Push of container images to **Docker Hub**
- Automatic builds on every push to `main`

---

## 📁 Project Structure

│
├── index.js
├── package.json
├── Dockerfile
├── README.md
└── .github/
└── workflows/
└── docker-build-push.yml
